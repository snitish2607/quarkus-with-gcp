Commands to install External Secrets Operator from Helm Charts:
1) To register the repository - helm repo add external-secrets https://charts.external-secrets.io
2) To fetch the latest index - helm repo update
3) To install external-secrets - helm install external-secrets external-secrets/external-secrets -n external-secrets --create-namespace --set installCRDs=true